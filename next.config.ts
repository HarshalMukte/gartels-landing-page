import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['www.launchuicomponents.com', 'assets.aceternity.com', 'images.unsplash.com'],
  },
};

export default nextConfig;
