import { LetterLoader } from "@/components/loaders/text-loader";
import React from "react";

const Loading = () => {
  return (
    <div>
      <LetterLoader />
    </div>
  );
};

export default Loading;
